# OneHub backend — single dependency file (FastAPI API, CLI, utils).
# Install: pip install -r requirements.txt
#
# Note: pydantic>=2.12 ships pydantic-core wheels for Python 3.14 on Windows.
# Older pydantic 2.5.x forced a source build of pydantic-core (needs MSVC/Rust).

# Web / API
fastapi==0.115.6
uvicorn==0.32.1
python-multipart==0.0.20
pydantic==2.12.5
python-dotenv==1.0.0
PyJWT==2.10.1
requests==2.32.3

# App libraries
python-docx>=1.0.0
reportlab>=4.0.0
pandas>=1.5.0
groq>=0.4.0
google-auth-oauthlib>=1.0.0
google-auth-httplib2>=0.1.0
google-api-python-client>=2.0.0
psycopg2-binary>=2.9.0
