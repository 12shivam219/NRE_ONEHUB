# ⚡ npm optimization settings to speed up installations and operations
# Reduce network timeouts to fail faster on problematic connections
fetch-timeout=60000
fetch-retry-mintimeout=20000
fetch-retry-maxtimeout=120000

# Prefer offline cached packages
prefer-offline=true

# Reduce package verbosity
loglevel=warn
